name: Deploy to Staging

# Deploys the trunk to the staging/QA VPS. The BRANCH is `main` (the single
# trunk, SAN-444); the ENVIRONMENT is still "staging" (the QA VPS).
on:
  push:
    branches: [main]
  workflow_dispatch: {}

concurrency:
  group: deploy-staging
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy ${{ github.sha }} to staging VPS
    runs-on: ubuntu-latest
    environment:
      name: staging
      url: ${{ vars.HEALTH_URL }}
    steps:
      - name: Checkout (need scripts/upsert-env.py + scripts/load-secrets-from-env.sh for scp)
        uses: actions/checkout@v7

      - name: Setup SSH agent
        uses: webfactory/ssh-agent@v0.10.0
        with:
          ssh-private-key: ${{ secrets.VPS_SSH_KEY }}

      - name: Add VPS host to known_hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H "${{ secrets.VPS_HOST }}" >> ~/.ssh/known_hosts

      - name: Deploy via SSH
        id: deploy
        env:
          # ── Deploy infra ──────────────────────────────────────────────
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
          DEPLOY_PATH: ${{ vars.DEPLOY_PATH || '~/.openclaw' }}
          SHA: ${{ github.sha }}
          ENABLE_YALC_SERVICE: ${{ vars.ENABLE_YALC_SERVICE || '1' }}
          ENABLE_OD_SERVICE: ${{ vars.ENABLE_OD_SERVICE || '1' }}
          RUN_DB_MIGRATIONS: ${{ vars.RUN_DB_MIGRATIONS }}
          # YALC ships as a public GHCR image (SAN-135). Override the tag here
          # to pin a version; empty falls back to the compose default (:edge).
          YALC_IMAGE: ${{ vars.YALC_IMAGE }}
          # ── Tier 1 — Auth & infra secrets ─────────────────────────────
          NEXTAUTH_SECRET: ${{ secrets.NEXTAUTH_SECRET }}
          ENCRYPTION_KEY: ${{ secrets.ENCRYPTION_KEY }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          OD_API_TOKEN: ${{ secrets.OD_API_TOKEN }}
          OPENCLAW_GATEWAY_TOKEN: ${{ secrets.OPENCLAW_GATEWAY_TOKEN }}
          MC_CHAT_SECRET: ${{ secrets.MC_CHAT_SECRET }}
          GTM_OS_API_TOKEN: ${{ secrets.GTM_OS_API_TOKEN }}
          YALC_API_TOKEN: ${{ secrets.YALC_API_TOKEN }}
          SANCHO_REPLY_WEBHOOK_TOKEN: ${{ secrets.SANCHO_REPLY_WEBHOOK_TOKEN }}
          YALC_REPO_TOKEN: ${{ secrets.YALC_REPO_TOKEN }}
          GHCR_USERNAME: ${{ github.actor }}
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          SLACK_CLIENT_ID: ${{ secrets.SLACK_CLIENT_ID }}
          SLACK_CLIENT_SECRET: ${{ secrets.SLACK_CLIENT_SECRET }}
          SLACK_SIGNING_SECRET: ${{ secrets.SLACK_SIGNING_SECRET }}
          # ── Tier 2 — System APIs ──────────────────────────────────────
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          FIREWORKS_API_KEY: ${{ secrets.FIREWORKS_API_KEY }}
          FIREWORKS_ACCOUNT_ID: ${{ secrets.FIREWORKS_ACCOUNT_ID || vars.FIREWORKS_ACCOUNT_ID }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          XAI_API_KEY: ${{ secrets.XAI_API_KEY }}
          MINIMAX_API_KEY: ${{ secrets.MINIMAX_API_KEY }}
          BRAVE_API_KEY: ${{ secrets.BRAVE_API_KEY }}
          APIFY_API_KEY: ${{ secrets.APIFY_API_KEY }}
          SCRAPECREATORS_API_KEY: ${{ secrets.SCRAPECREATORS_API_KEY }}
          SCRAPECREATORS_TIMEOUT_MS: ${{ vars.SCRAPECREATORS_TIMEOUT_MS || '30000' }}
          PARTNERSHIPS_LIVE_DISCOVERY_TIMEOUT_MS: ${{ vars.PARTNERSHIPS_LIVE_DISCOVERY_TIMEOUT_MS || '300000' }}
          PARTNERSHIPS_LIVE_DISCOVERY_MAX_CANDIDATES: ${{ vars.PARTNERSHIPS_LIVE_DISCOVERY_MAX_CANDIDATES || '500' }}
          PARTNERSHIPS_LIVE_DISCOVERY_CONCURRENCY: ${{ vars.PARTNERSHIPS_LIVE_DISCOVERY_CONCURRENCY || '5' }}
          PARTNERSHIPS_DISCOVERY_EXECUTION_V2: ${{ vars.PARTNERSHIPS_DISCOVERY_EXECUTION_V2 || 'off' }}
          PARTNERSHIPS_DISCOVERY_V2_SLUGS: ${{ vars.PARTNERSHIPS_DISCOVERY_V2_SLUGS }}
          FIRECRAWL_API_KEY: ${{ secrets.FIRECRAWL_API_KEY }}
          SERPER_API_KEY: ${{ secrets.SERPER_API_KEY }}
          DATAFORSEO_LOGIN: ${{ secrets.DATAFORSEO_LOGIN }}
          DATAFORSEO_PASSWORD: ${{ secrets.DATAFORSEO_PASSWORD }}
          NOTION_API_KEY: ${{ secrets.NOTION_API_KEY }}
          FAL_API_KEY: ${{ secrets.FAL_API_KEY }}
          WAVESPEED_API_KEY: ${{ secrets.WAVESPEED_API_KEY }}
          DUMPLING_API_KEY: ${{ secrets.DUMPLING_API_KEY }}
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          LLM_USAGE_SLACK_BOT_TOKEN: ${{ secrets.LLM_USAGE_SLACK_BOT_TOKEN || vars.LLM_USAGE_SLACK_BOT_TOKEN }}
          LLM_USAGE_SLACK_CHANNEL: ${{ vars.LLM_USAGE_SLACK_CHANNEL || 'C0AHWST8Y5C' }}
          LLM_USAGE_SLACK_REPORT_HOUR: ${{ vars.LLM_USAGE_SLACK_REPORT_HOUR || '9' }}
          LLM_USAGE_SLACK_REPORT_MINUTE: ${{ vars.LLM_USAGE_SLACK_REPORT_MINUTE || '0' }}
          LLM_USAGE_SLACK_REPORT_TIMEZONE: ${{ vars.LLM_USAGE_SLACK_REPORT_TIMEZONE || 'Europe/Madrid' }}
          LLM_USAGE_SLACK_REPORT_ENABLED: ${{ vars.LLM_USAGE_SLACK_REPORT_ENABLED || '1' }}
          LLM_USAGE_SLACK_REQUIRE_BILLING: ${{ vars.LLM_USAGE_SLACK_REQUIRE_BILLING || '0' }}
          LLM_USAGE_SLACK_EXPECTED_BOT: ${{ vars.LLM_USAGE_SLACK_EXPECTED_BOT || 'SanchoCMO' }}
          LLM_USAGE_SLACK_ALLOW_DEFAULT_TOKEN: ${{ vars.LLM_USAGE_SLACK_ALLOW_DEFAULT_TOKEN || '1' }}
          LLM_USAGE_USD_TO_EUR: ${{ vars.LLM_USAGE_USD_TO_EUR || '0.92' }}
          INSTANTLY_API_KEY: ${{ secrets.INSTANTLY_API_KEY }}
          METRICOOL_API_KEY: ${{ secrets.METRICOOL_API_KEY }}
          SANCHO_INTERNAL_API_TOKEN: ${{ secrets.SANCHO_INTERNAL_API_TOKEN }}
          SANCHO_DOCS_ASSISTANT_TOKEN: ${{ secrets.SANCHO_DOCS_ASSISTANT_TOKEN }}
          SANCHO_MCP_TOKENS: ${{ secrets.SANCHO_MCP_TOKENS }}
          # ── Tier 3 — Skill APIs ───────────────────────────────────────
          APOLLO_API_KEY: ${{ secrets.APOLLO_API_KEY }}
          GHL_API_KEY: ${{ secrets.GHL_API_KEY }}
          GHL_LOCATION_ID: ${{ secrets.GHL_LOCATION_ID }}
          META_ACCESS_TOKEN: ${{ secrets.META_ACCESS_TOKEN }}
          META_AD_ACCOUNT_ID: ${{ secrets.META_AD_ACCOUNT_ID }}
          PAGESPEED_API_KEY: ${{ secrets.PAGESPEED_API_KEY }}
          # UNIPILE_API_KEY / UNIPILE_DSN are intentionally NOT injected here:
          # they are YALC provider credentials managed from the Sancho UI, which
          # writes them to YALC's own key store (a persistent volume). Injecting
          # them from secrets would override the UI choice on every deploy (the
          # container env wins over the key store at boot). Sancho has no direct
          # consumer of these vars — only YALC reads them. (SAN-36)
          DATAFORSEO_API_KEY: ${{ secrets.DATAFORSEO_API_KEY }}
          # ── R2 / Supabase / Cloudflare / Apify (secrets que vivían solo en el VPS) ──
          R2_UPLOAD_IMAGE_ACCESS_KEY_ID: ${{ secrets.R2_UPLOAD_IMAGE_ACCESS_KEY_ID }}
          R2_UPLOAD_IMAGE_SECRET_ACCESS_KEY: ${{ secrets.R2_UPLOAD_IMAGE_SECRET_ACCESS_KEY }}
          R2_UPLOAD_IMAGE_BUCKET_NAME: ${{ secrets.R2_UPLOAD_IMAGE_BUCKET_NAME || vars.R2_UPLOAD_IMAGE_BUCKET_NAME }}
          R2_PUBLIC_URL: ${{ secrets.R2_PUBLIC_URL || vars.R2_PUBLIC_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          APIFY_TOKEN: ${{ secrets.APIFY_TOKEN }}
          # ── Variables (non-sensitive config) ─────────────────────────
          BASE_URL: ${{ vars.BASE_URL }}
          NEXT_PUBLIC_ENV_LABEL: ${{ vars.NEXT_PUBLIC_ENV_LABEL }}
          NEXT_PUBLIC_GROWIE_SUPPORT_ENABLED: ${{ vars.NEXT_PUBLIC_GROWIE_SUPPORT_ENABLED || '0' }}
          NEXTAUTH_URL: ${{ vars.NEXTAUTH_URL }}
          OD_WEB_URL: ${{ vars.OD_WEB_URL }}
          OD_ALLOWED_ORIGINS: ${{ vars.OD_ALLOWED_ORIGINS }}
          OPEN_DESIGN_IMAGE: ${{ vars.OPEN_DESIGN_IMAGE }}
          MC_CHAT_GATEWAY: ${{ vars.MC_CHAT_GATEWAY }}
          SLACK_REDIRECT_URI: ${{ vars.SLACK_REDIRECT_URI }}
          ANTHROPIC_AUTH_MODE: ${{ vars.ANTHROPIC_AUTH_MODE }}
          OPENAI_AUTH_MODE: ${{ vars.OPENAI_AUTH_MODE }}
          ADMIN_EMAIL_DOMAIN: ${{ vars.ADMIN_EMAIL_DOMAIN }}
          ADMIN_IDENTITY_EMAIL: ${{ vars.ADMIN_IDENTITY_EMAIL }}
          # Force the Neon HTTP driver in G4U deploys. The runtime also
          # auto-detects neon from the *.neon.tech host, but pinning it here
          # removes any chance of falling back to the vanilla-Postgres driver
          # (used by the bundled local-db) if DATABASE_URL ever changes shape.
          DATABASE_DRIVER: neon
          SANCHO_MCP_AUDIT_BACKEND: ${{ vars.SANCHO_MCP_AUDIT_BACKEND }}
          SANCHO_MCP_AUDIT_FAIL_CLOSED: ${{ vars.SANCHO_MCP_AUDIT_FAIL_CLOSED }}
          SANCHO_MCP_AUDIT_FILE: ${{ vars.SANCHO_MCP_AUDIT_FILE }}
          # Sancho->YALC runtime URL inside the compose network. Default avoids the
          # client.ts localhost:3847 fallback that left the cockpit stuck on "Checking".
          YALC_BASE_URL: ${{ vars.YALC_BASE_URL || 'http://yalc:3847' }}
          # YALC->Sancho reply callback URL inside the compose network.
          SANCHO_REPLY_WEBHOOK_URL: ${{ vars.SANCHO_REPLY_WEBHOOK_URL || 'http://sanchocmo:3000/api/yalc/replies' }}
        run: |
          # Build a base64-encoded JSON dict of every non-empty key (skip empty
          # values so unset GitHub secrets/variables are safe no-ops on the VPS).
          PAYLOAD_B64=$(python3 - <<'PY'
          import os, json, base64
          KEYS = [
              # Tier 1 — Auth & infra
              "NEXTAUTH_SECRET", "ENCRYPTION_KEY", "DATABASE_URL",
              "GOOGLE_CLIENT_ID", "GOOGLE_CLIENT_SECRET",
              "OD_API_TOKEN", "OPENCLAW_GATEWAY_TOKEN", "MC_CHAT_SECRET", "GTM_OS_API_TOKEN",
              "YALC_API_TOKEN", "SANCHO_REPLY_WEBHOOK_TOKEN", "CLAUDE_CODE_OAUTH_TOKEN",
              "SLACK_CLIENT_ID", "SLACK_CLIENT_SECRET", "SLACK_SIGNING_SECRET",
              # Tier 2 — System APIs
              "ANTHROPIC_API_KEY", "OPENROUTER_API_KEY", "FIREWORKS_API_KEY", "FIREWORKS_ACCOUNT_ID",
              "OPENAI_API_KEY", "GEMINI_API_KEY", "XAI_API_KEY", "MINIMAX_API_KEY",
              "BRAVE_API_KEY", "APIFY_API_KEY", "SCRAPECREATORS_API_KEY",
              "FIRECRAWL_API_KEY", "SERPER_API_KEY", "DATAFORSEO_LOGIN", "DATAFORSEO_PASSWORD",
              "NOTION_API_KEY", "FAL_API_KEY",
              "WAVESPEED_API_KEY", "DUMPLING_API_KEY", "SLACK_BOT_TOKEN", "LLM_USAGE_SLACK_BOT_TOKEN",
              "LLM_USAGE_SLACK_CHANNEL", "LLM_USAGE_SLACK_REPORT_HOUR", "LLM_USAGE_SLACK_REPORT_MINUTE",
              "LLM_USAGE_SLACK_REPORT_TIMEZONE", "LLM_USAGE_SLACK_REPORT_ENABLED", "LLM_USAGE_SLACK_REQUIRE_BILLING",
              "LLM_USAGE_SLACK_EXPECTED_BOT", "LLM_USAGE_SLACK_ALLOW_DEFAULT_TOKEN", "LLM_USAGE_USD_TO_EUR",
              "INSTANTLY_API_KEY", "METRICOOL_API_KEY", "SANCHO_INTERNAL_API_TOKEN",
              "SANCHO_DOCS_ASSISTANT_TOKEN",
              "SANCHO_MCP_TOKENS",
              # Tier 3 — Skill APIs
              "APOLLO_API_KEY", "GHL_API_KEY", "GHL_LOCATION_ID",
              "META_ACCESS_TOKEN", "META_AD_ACCOUNT_ID", "PAGESPEED_API_KEY",
              # UNIPILE_* omitted on purpose — UI-managed via YALC key store (SAN-36)
              "DATAFORSEO_API_KEY",
              # R2 / Supabase / Cloudflare / Apify
              "R2_UPLOAD_IMAGE_ACCESS_KEY_ID", "R2_UPLOAD_IMAGE_SECRET_ACCESS_KEY",
              "R2_UPLOAD_IMAGE_BUCKET_NAME", "R2_PUBLIC_URL",
              "SUPABASE_SERVICE_ROLE_KEY", "SUPABASE_ANON_KEY",
              "CLOUDFLARE_ACCOUNT_ID", "APIFY_TOKEN",
              # Variables (non-sensitive config)
              "BASE_URL", "NEXT_PUBLIC_ENV_LABEL", "NEXT_PUBLIC_GROWIE_SUPPORT_ENABLED", "NEXTAUTH_URL",
              "OD_WEB_URL", "OD_ALLOWED_ORIGINS", "OPEN_DESIGN_IMAGE", "YALC_IMAGE",
              "MC_CHAT_GATEWAY", "SLACK_REDIRECT_URI",
              "ANTHROPIC_AUTH_MODE", "OPENAI_AUTH_MODE",
              "ADMIN_EMAIL_DOMAIN", "ADMIN_IDENTITY_EMAIL", "DATABASE_DRIVER",
              "SANCHO_MCP_AUDIT_BACKEND", "SANCHO_MCP_AUDIT_FAIL_CLOSED",
              "SANCHO_MCP_AUDIT_FILE",
              "YALC_BASE_URL", "SANCHO_REPLY_WEBHOOK_URL",
              "SCRAPECREATORS_TIMEOUT_MS", "PARTNERSHIPS_LIVE_DISCOVERY_TIMEOUT_MS",
              "PARTNERSHIPS_LIVE_DISCOVERY_MAX_CANDIDATES", "PARTNERSHIPS_LIVE_DISCOVERY_CONCURRENCY",
              "PARTNERSHIPS_DISCOVERY_EXECUTION_V2", "PARTNERSHIPS_DISCOVERY_V2_SLUGS",
          ]
          out = {k: os.environ[k] for k in KEYS if os.environ.get(k)}
          print(base64.b64encode(json.dumps(out).encode()).decode())
          PY
          )

          # Ship the tested upsert script to the VPS so the value-application
          # logic is single-sourced (no inline duplication in this workflow).
          scp -o StrictHostKeyChecking=no scripts/upsert-env.py "$VPS_USER@$VPS_HOST:/tmp/upsert-env.py"

          ssh "$VPS_USER@$VPS_HOST" "DEPLOY_PATH=$DEPLOY_PATH SHA=$SHA PAYLOAD_B64=$PAYLOAD_B64 ENABLE_YALC_SERVICE=$ENABLE_YALC_SERVICE RUN_DB_MIGRATIONS=$RUN_DB_MIGRATIONS YALC_REPO_TOKEN=$YALC_REPO_TOKEN GHCR_USERNAME=$GHCR_USERNAME bash -s" <<'EOF'
            set -euo pipefail
            cd "$DEPLOY_PATH"

            # SAN-445: back up + clear untracked working-tree paths that collide
            # with a path the target ref tracks (e.g. the specialist-agent
            # workspace-<agent>/{brand,_system} symlinks added by SAN-438). The
            # `git stash --include-untracked` below already covers this on
            # staging, but this guard makes the checkout robust if that logic
            # ever narrows — and keeps staging/prod behaviourally in sync. Moves
            # (never rm) each colliding path to .deploy-collision-backups/<ts>/;
            # no-op when the tree is clean.
            clear_checkout_collisions() {
              local ref="$1" bk tt th p cleared=0
              bk=".deploy-collision-backups/$(date -u +%Y%m%dT%H%M%SZ)"
              tt="$(mktemp)"; th="$(mktemp)"
              git ls-tree -r --name-only "$ref" | sort > "$tt"
              git ls-files | sort > "$th"
              while IFS= read -r p; do
                [ -n "$p" ] || continue
                { [ -e "$p" ] || [ -L "$p" ]; } || continue
                git ls-files --error-unmatch "$p" >/dev/null 2>&1 && continue
                mkdir -p "$bk/$(dirname "$p")"
                mv "$p" "$bk/$p"
                echo "▶ SAN-445: backed up + cleared colliding untracked path: $p → $bk/$p"
                cleared=$((cleared+1))
              done < <(comm -23 "$tt" "$th")
              rm -f "$tt" "$th"
              [ "$cleared" -gt 0 ] && echo "▶ SAN-445: cleared $cleared colliding path(s) into $bk"
              return 0
            }

            echo "▶ Fetching main…"
            git fetch origin main --prune

            # Backup `workspace-sancho/brand/<slug>/` folders before checkout.
            # Historically these were committed to git; the main trunk is
            # transitioning them to untracked-runtime via `git rm --cached`.
            # On the first deploy after that transition, `git checkout <SHA>`
            # would delete the on-disk files (they're in old HEAD but not new
            # HEAD). On subsequent deploys this is a no-op (folders are
            # gitignored and untouched by checkout). The restore below puts
            # them back if checkout removed them.
            BRAND_BACKUP_DIR="$(mktemp -d -t sancho-brand-bk-XXXXXX)"
            if [ -d workspace-sancho/brand ]; then
              echo "▶ Backing up workspace-sancho/brand/ runtime data to $BRAND_BACKUP_DIR…"
              for d in workspace-sancho/brand/*/; do
                [ -d "$d" ] || continue
                name=$(basename "$d")
                # Skip the always-tracked sample and the archive bucket.
                [ "$name" = "example" ] && continue
                [ "$name" = "_archived" ] && continue
                cp -a "$d" "$BRAND_BACKUP_DIR/"
              done
            fi

            if ! git diff --quiet || ! git diff --cached --quiet || [ -n "$(git ls-files --others --exclude-standard)" ]; then
              echo "▶ Stashing local runtime changes before checkout…"
              git stash push --include-untracked -m "deploy-staging-$(date -u +%Y%m%dT%H%M%SZ)" || true
            fi

            # Apply GitHub `staging` Environment secrets + variables to ~/.openclaw/.env
            # via the tested upsert script. Hash before/after lets us conditionally
            # force-recreate the container on env-only changes (workflow_dispatch
            # re-runs without a code commit otherwise wouldn't pick up new values).
            ENV_HASH_BEFORE=$(sha256sum .env 2>/dev/null | cut -d' ' -f1 || echo "missing")
            if [ -n "${PAYLOAD_B64:-}" ]; then
              echo "▶ Upserting envs from GitHub \`staging\` Environment…"
              echo "$PAYLOAD_B64" | python3 /tmp/upsert-env.py .env --preserve-existing ANTHROPIC_API_KEY
            fi
            ENV_HASH_AFTER=$(sha256sum .env | cut -d' ' -f1)
            ENV_CHANGED=0
            if [ "$ENV_HASH_BEFORE" != "$ENV_HASH_AFTER" ]; then
              ENV_CHANGED=1
              echo "▶ .env content changed (will force-recreate sanchocmo)"
            fi
            rm -f /tmp/upsert-env.py

            echo "▶ Checking out $SHA…"
            # Self-heal: discard stray local edits under src/ that would abort the
            # checkout. Runtime state under agents/ and workspace-*/ is preserved.
            git checkout -- src 2>/dev/null || true
            clear_checkout_collisions "$SHA"
            git checkout "$SHA"
            export GIT_COMMIT="$(git rev-parse HEAD)"

            # Restore brand/<slug>/ files that the checkout removed.
            # The previous "skip if target non-empty" approach lost data: an
            # active client whose untracked runtime files survived the checkout
            # (chat, projects with in-progress tasks) would block the restore
            # of its tracked files (foundation-state.json, brand-book/...).
            # Use `cp -a -n` (no-clobber) instead — copies any backup file
            # that isn't already in the target. Idempotent and safe.
            if [ -d "$BRAND_BACKUP_DIR" ]; then
              for d in "$BRAND_BACKUP_DIR"/*/; do
                [ -d "$d" ] || continue
                name=$(basename "$d")
                target="workspace-sancho/brand/$name"
                if [ -d "$target" ] && [ -n "$(ls -A "$target" 2>/dev/null || true)" ]; then
                  # Target survived with some files. Merge missing ones in.
                  before=$(find "$target" -type f 2>/dev/null | wc -l)
                  cp -a -n "$d." "$target/" 2>/dev/null || true
                  after=$(find "$target" -type f 2>/dev/null | wc -l)
                  diff=$((after - before))
                  if [ "$diff" -gt 0 ]; then
                    echo "▶ Merged $diff missing file(s) into workspace-sancho/brand/$name from backup"
                  fi
                else
                  echo "▶ Restoring workspace-sancho/brand/$name from backup…"
                  mkdir -p "$target"
                  cp -a "$d." "$target/"
                fi
              done
              rm -rf "$BRAND_BACKUP_DIR"
            fi
            COMPOSE_ARGS="-f docker-compose.yml"
            # Open Design is an opt-in overlay (image is public). G4U keeps it on by default.
            [ "${ENABLE_OD_SERVICE:-1}" != "0" ] && COMPOSE_ARGS="$COMPOSE_ARGS -f docker-compose.od.yml"
            # YALC (Outreach) is an opt-in overlay; its image is public on GHCR
            # (pulled below via `pull --ignore-buildable`, same as Open Design).
            [ "${ENABLE_YALC_SERVICE:-1}" != "0" ] && COMPOSE_ARGS="$COMPOSE_ARGS -f docker-compose.yalc.yml"
            echo "▶ Building and starting containers (GIT_COMMIT=$GIT_COMMIT)…"
            docker compose $COMPOSE_ARGS build --pull
            SANCHOCMO_RESOLVED_IMAGE="$(docker compose $COMPOSE_ARGS config --format json | python3 -c 'import json,sys; print(json.load(sys.stdin)["services"]["sanchocmo"]["image"])')"
            export SANCHOCMO_IMAGE_DIGEST="$(docker image inspect "$SANCHOCMO_RESOLVED_IMAGE" --format '{{.Id}}')"
            if [ -z "$SANCHOCMO_IMAGE_DIGEST" ]; then
              echo "::error::Could not resolve the built sanchocmo image ID."
              exit 1
            fi
            echo "▶ Runtime image digest: $SANCHOCMO_IMAGE_DIGEST"
            # Agent-run storage selects Postgres whenever DATABASE_URL exists.
            # Apply its additive/idempotent schema unconditionally before the
            # replacement application container is allowed to start.
            echo "▶ Ensuring durable agent-run schema…"
            docker compose $COMPOSE_ARGS run -T --rm --no-deps --entrypoint bash sanchocmo -lc "cd /app/mc-nextjs && npm run db:migrate:agent-runs" < /dev/null
            echo "▶ Ensuring durable execution-control schema…"
            docker compose $COMPOSE_ARGS run -T --rm --no-deps --entrypoint bash sanchocmo -lc "cd /app/mc-nextjs && npm run db:migrate:execution-control" < /dev/null
            if [ "${RUN_DB_MIGRATIONS:-0}" = "1" ]; then
              echo "▶ Running DB migrations…"
              # The repo's historical SQL migrations are not a safe input for
              # `drizzle-kit migrate` yet: there is no Drizzle journal in git,
              # and one old migration drops/recreates `tasks`. Apply only the
              # curated additive deploy migrations via the non-destructive
              # single-file runner.
              # Redirect stdin so the one-off container cannot consume the
              # remaining SSH heredoc before `docker compose up` runs.
              docker compose $COMPOSE_ARGS run -T --rm --no-deps --entrypoint bash sanchocmo -lc "cd /app/mc-nextjs && npm run db:migrate:deploy" < /dev/null
            fi
            # `build --pull` only refreshes images for services with a
            # `build:` directive (sanchocmo). Services that consume a
            # pre-built image (open-design via OPEN_DESIGN_IMAGE) need an
            # explicit pull, or Docker keeps the cached local image even
            # when the rolling tag (`:sanchocmo`, `:edge`) points at a new
            # digest in the registry. Seen on 2026-05-20: a fresh branded
            # OD image with the `claude` CLI baked in shipped to GHCR but
            # staging kept the prior `:sanchocmo` digest until a manual
            # `docker compose pull` was run via SSH.
            if [ -n "${YALC_REPO_TOKEN:-}" ]; then
              echo "▶ Logging in to GHCR for private deploy images…"
              printf '%s' "$YALC_REPO_TOKEN" | docker login ghcr.io -u "${GHCR_USERNAME:-deploy}" --password-stdin >/dev/null
            fi
            docker compose $COMPOSE_ARGS pull --ignore-buildable || true
            # `--wait` blocks until every service with a healthcheck is
            # `healthy` (or the timeout fires), so the deploy step fails fast
            # when the new container can't actually serve traffic — instead
            # of returning success the moment containers are *created*. This
            # is what lets the workflow report a real outcome and is the
            # cheap path to minimizing user-visible downtime on staging:
            # paired with the tightened sanchocmo healthcheck (10s interval),
            # the new container is acknowledged as ready within ~10s of the
            # app booting, vs. up to 60s with the old interval.
            UP_ARGS="--wait --wait-timeout 180"
            if [ "$ENV_CHANGED" = "1" ]; then
              UP_ARGS="$UP_ARGS --force-recreate"
            fi
            # Rebuilds tag the local image as `openclaw-sanchocmo:latest`, but
            # Compose can keep the existing container when the service config is
            # unchanged. Force-recreate `sanchocmo` so health checks validate
            # the newly built image, not yesterday's still-healthy container.
            if ! docker compose $COMPOSE_ARGS up -d --no-deps --force-recreate $UP_ARGS sanchocmo; then
              echo "▶ docker compose up sanchocmo failed; current service state:"
              docker compose $COMPOSE_ARGS ps || true
              echo "▶ sanchocmo logs:"
              docker compose $COMPOSE_ARGS logs --no-color --tail=200 sanchocmo || true
              exit 1
            fi
            if ! docker compose $COMPOSE_ARGS up -d $UP_ARGS; then
              echo "▶ docker compose up failed; current service state:"
              docker compose $COMPOSE_ARGS ps || true
              echo "▶ yalc logs:"
              docker compose $COMPOSE_ARGS logs --no-color --tail=200 yalc || true
              echo "▶ sanchocmo logs:"
              docker compose $COMPOSE_ARGS logs --no-color --tail=200 sanchocmo || true
              exit 1
            fi
            docker compose $COMPOSE_ARGS ps
          EOF


          COMPOSE_FILES="-f docker-compose.yml"
          [ "${ENABLE_OD_SERVICE:-1}" != "0" ] && COMPOSE_FILES="$COMPOSE_FILES -f docker-compose.od.yml"
          [ "${ENABLE_YALC_SERVICE:-1}" != "0" ] && COMPOSE_FILES="$COMPOSE_FILES -f docker-compose.yalc.yml"
          RUNNING_IMAGE_ID="$(ssh "$VPS_USER@$VPS_HOST" "cd $DEPLOY_PATH && cid=\$(docker compose $COMPOSE_FILES ps -q sanchocmo) && [ -n \"\$cid\" ] && docker inspect \"\$cid\" --format '{{.Image}}'" | tr -d '[:space:]')"
          if [ -z "$RUNNING_IMAGE_ID" ]; then
            echo "::error::Could not inspect the running sanchocmo image after deploy."
            exit 1
          fi
          echo "image_id=$RUNNING_IMAGE_ID" >> "$GITHUB_OUTPUT"

      - name: Health check
        env:
          HEALTH_URL: ${{ vars.HEALTH_URL }}
          EXPECTED_COMMIT: ${{ github.sha }}
          EXPECTED_IMAGE_ID: ${{ steps.deploy.outputs.image_id }}
        run: |
          if [ -z "$HEALTH_URL" ]; then
            echo "::error::HEALTH_URL variable not set on staging environment; deploy identity cannot be verified"
            exit 1
          fi
          echo "▶ Polling $HEALTH_URL (up to 60s)…"
          for i in {1..30}; do
            body="$(curl -fsS --max-time 5 "$HEALTH_URL" || true)"
            if [ -n "$body" ]; then
              read -r ok commit image_digest < <(python3 -c 'import json,sys; d=json.load(sys.stdin); print(str(bool(d.get("ok"))).lower(), d.get("commit") or "", d.get("imageDigest") or "")' <<<"$body" 2>/dev/null || true)
              if [ "$ok" = "true" ] && [ "$commit" = "$EXPECTED_COMMIT" ] && [ "$image_digest" = "$EXPECTED_IMAGE_ID" ]; then
                echo "✓ Health check passed at commit $commit ($image_digest)"
                exit 0
              fi
              echo "▶ Health responded ok='${ok:-missing}' commit='${commit:-missing}' image='${image_digest:-missing}', expected commit '$EXPECTED_COMMIT' image '$EXPECTED_IMAGE_ID'"
            fi
            sleep 2
          done
          echo "::error::Health check failed after 60s — check container logs on the staging VPS"
          exit 1
