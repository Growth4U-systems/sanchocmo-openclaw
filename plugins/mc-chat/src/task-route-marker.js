// OpenClaw plugin compatibility shim. The implementation lives in Sancho's
// runtime-neutral agent contract so every adapter can share the protocol.
export { parseTaskRouteMarkers } from "../../../src/lib/runtime/agent-contract/task-route-marker.mjs";
