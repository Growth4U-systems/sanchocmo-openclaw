# 2026-03-24

## Dashboard Improvements (Mar 23)

### Date Range Selector
- Added 4 buttons: Ayer | 7 días | 30 días | Todo (defaults to 7 días)
- Client-side aggregation: sums counts (sessions, spend, contacts), averages rates (CTR, bounce, CPC)
- Feeds (recentLead, postDetail, conversations) deduplicated, show latest
- No extra API calls — aggregates from rolling data already loaded
- Example: Ad Spend Ayer €161 vs 7 días €991 vs Todo €1,299

### Sortable Ad Tables
- All 3 ads tables (Campaigns, Ad Sets, Ad Creatives) now have clickable column headers
- Click to sort descending, click again for ascending
- Arrow indicators ⇅ / ▲ / ▼
- Uses `data-sort` attributes on TDs for numeric sorting
- `sortAdsTable()` function handles the sort logic

### Leads + CPL Columns
- Added Leads and CPL (Cost Per Lead) columns to all ads tables
- CPL = spend ÷ leads per ad/adset/campaign
- Green highlight for leads > 0 and CPL < €15
- Example data: Angulo 1_V1 = 7 leads, CPL €27.86

### Meetings Fix (Pipeline-Based)
- GHL `/calendars/events` endpoint returns 0 events with Private Integration Tokens (API bug/limitation)
- **Fix**: Count meetings from **pipeline Closer** instead — opportunities in stages matching keywords: "agendad", "confirmad", "seguimiento", "demo", "meeting", "llamada" (excluding "no asist", "cancelad")
- Result: 4 meetings (was 0): Llamada Agendada 2, Confirmada 1, Seguimiento 1

### Per-Channel View in Metrics Plan
- New "By Channel" tab in the Plan card (alongside "Overview")
- Crosses data from GHL (leads by channel attribution), Meta Ads (spend), GA4 (sessions by channel)
- Channels: Facebook Ads, Instagram Organic, Calendar Booking, Organic Search, Organic Social
- Shows: Leads, Sessions, Spend, CPL per channel
- `channelMap` maps GHL attribution strings to friendly names

### GHL Adapter Updates
- New contacts now tracked **per channel** via `attributions[0].medium/utmSessionSource`
- Metrics output includes `newContacts` with `dimensions: { channel }` per attribution channel
- Source breakdown still available for total contact base

### Ads Table Styling Fix
- Added `sortable-ads-table` CSS class: compact 12px font, proper padding, hover rows, horizontal scroll on overflow
- Fixed visual issues Alfonso reported

### Alfonso's Next Request (pending)
- Wants the metrics plan template to include per-channel funnel view built-in (not just dashboard)
- Each channel (Facebook, Google, Outbound, etc.) should show its own leads → meetings → deals funnel
- This is a template/schema change in `integration-mappings.json` + `generate-plan.js`

### Key Identifiers
- Closer Pipeline ID: `OSBqHAuARcSvYct49emn`
- Meeting stage IDs: `9c385e34` (Agendada), `749376a8` (Confirmada), `3b60296e` (Seguimiento)
- Calendar Group: `DVPVoxnzcrhQstlpOIN5` (Llamada Estratégica)
- User IDs: Alfonso `5qdCQfJvnYrSWHjUL6jX`, Martin `Z4ZnDTKQJwoQfalW7fxl`
- Growth4U has 250 total contacts, 31 total leads from ads, 5 pipeline opportunities
